{
  "name": "@rapid/example-app",
  "version": "1.0.0",
  "private": true,
  "description": "Example application demonstrating config usage",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist cdk.out",
    "type-check": "tsc --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "@rapid/cdk": "workspace:*",
    "@rapid/config": "workspace:*",
    "@rapid/constants": "workspace:*",
    "aws-cdk-lib": "^2.172.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "aws-cdk": "^2.172.0",
    "typescript": "^5.7.2"
  }
}
